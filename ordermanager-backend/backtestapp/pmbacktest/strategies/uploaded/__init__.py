"""User-authored strategies saved from the dashboard or placed here manually.

Each ``*.py`` file (except ``__init__.py``) must define exactly one subclass of
:class:`~pmbacktest.strategies.base.Strategy`. The strategy id is the file stem
(e.g. ``my_alpha.py`` → id ``my_alpha``), matching what you select in the GUI.
"""
