/**
 * Performance toggles for the SPA.
 *
 * `ENABLE_JOB_CHARTS`: Recharts / heavy market replay on the job detail page.
 * Set to `true` when you want full graphs back (slower, especially on large equity series).
 */
export const ENABLE_JOB_CHARTS = false;
